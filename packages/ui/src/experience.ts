/**
 * The assembled presentation layer. Needs both `framer-motion` and `lenis`.
 *
 * One import for an app that wants the full Noirly treatment; drop to
 * `@noirly-dev/ui`, `/motion` and `/scroll` to assemble the parts by hand.
 */

export {
  NoirlyExperience,
  type NoirlyExperienceProps,
} from "./components/fx/noirly-experience.js";
